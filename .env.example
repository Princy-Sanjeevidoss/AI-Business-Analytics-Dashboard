# Database settings (Defaults to SQLite)
DATABASE_URL=sqlite:///database/business.db

# Security keys (Change this in production)
SECRET_KEY=dev-secret-key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# AI Configuration (Optional, chatbot uses database fallback if empty)
OPENAI_API_KEY=
